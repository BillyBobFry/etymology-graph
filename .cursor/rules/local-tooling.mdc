---
description: Local Node and package manager setup for this repo
alwaysApply: true
---

# Local Tooling

- The repo pins Node with `.tool-versions`; current target is `nodejs 26.0.0` with `nodejs 24.15.0` as an asdf fallback for the local `sfw` package manager wrapper.
- Use pnpm for workspace commands. The normal `pnpm ...` command should work; if the shell wrapper fails, run `command pnpm ...` or `asdf exec pnpm ...`.
- `pnpm@10.19.0` is installed into the local Node 26 asdf toolchain.
- Prefer root scripts such as `pnpm typecheck`, `pnpm seed:extract:structured-ancestry`, `pnpm import:batch-preview:structured`, `pnpm import:db:structured`, and `pnpm embeddings:refresh:english`.
- `pnpm import:db:structured` refreshes `graph_edge_walk_mv` after full imports because API graph endpoints read edge data from that materialized view. Runs with `IMPORT_LIMIT_RECORDS` skip the refresh by default; set `IMPORT_REFRESH_GRAPH_EDGE_WALK=true` to force it.
- `pnpm embeddings:refresh:english` scans English `graph_nodes`, calls OpenAI embeddings, and upserts `term_embeddings`. It requires `OPENAI_API_KEY` and `DATABASE_URL`; use `TERM_EMBEDDING_LIMIT` for paid-call smoke tests.

## Local Database

- Use `docker-compose.yml` for the local Postgres 16 database with pgvector support; start it with `docker compose up -d`.
- The local database connection is `postgres://postgres:postgres@localhost:5432/etymology_graph`, matching `.env.example`.
- The Docker service is `postgres` and the container name is `etymology-graph-postgres`; the database data persists in the `postgres_data` Docker volume.
- If local Postgres client tools are unavailable, run `psql` through the container, for example `docker exec etymology-graph-postgres psql -U postgres -d etymology_graph`.
- Apply every numbered migration in `db/migrations/` in order before running imports. Migration `011` enables pgvector and creates `term_embeddings`.
- For pgAdmin desktop, connect to host `localhost`, port `5432`, database `etymology_graph`, username `postgres`, password `postgres`. If pgAdmin runs in Docker, use host `host.docker.internal`.
